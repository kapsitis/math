Naive string search: Define a function to receive two lists of chars (text and pattern).
It returns the third list - all entries of the string into another string. 

Define also the prefix function - it receives list of strings (and returns prefix function as a list). 